services:
  api.gateway:
    container_name: api.gateway
    build: 
      context: .
    volumes: 
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./.config:/etc/jwt_checker/.config
      - ./.urls:/etc/nginx/.urls
    ports:
      - '80:80'
